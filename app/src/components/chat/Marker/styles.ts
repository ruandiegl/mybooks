import { StyleSheet } from 'react-native';
import { theme } from '../../../styles/theme';
export const styles = StyleSheet.create({ row: { alignItems: 'center', marginVertical: theme.spacing.sm }, text: { color: theme.colors.mutedForeground, backgroundColor: theme.colors.background, paddingHorizontal: theme.spacing.sm, fontFamily: theme.typography.medium, fontSize: 11 } });
